<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bg_default"
        android:orientation="vertical"
        tools:context="io.github.vnicius.twitterclone.ui.result.SearchResultActivity">

    <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar_search_result"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/white"
            android:theme="@style/AppTheme.ToolbarColoredBackArrow">

        <include layout="@layout/partial_search_field" />
    </android.support.v7.widget.Toolbar>

    <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_search_result_tweets_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            tools:listitem="@layout/item_tweet" />

    <TextView
            android:id="@+id/tv_search_result_no_result_message"
            style="@style/TextAppearance.AppCompat.TextAppearanceNormal.Secondary"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:padding="@dimen/spacing_normal"
            android:textAlignment="center"
            android:visibility="gone"
            tools:text="No results for" />

    <include
            android:id="@+id/inc_search_result_spinner"
            layout="@layout/partial_spinner"
            android:visibility="gone" />

    <include
            android:id="@+id/inc_search_result_connection_error"
            layout="@layout/partial_connection_error"
            android:visibility="gone" />

</LinearLayout>
